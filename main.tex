% ============================================================
% FOR OVERLEAF: Uncomment the line below and comment out the
% \documentclass{article} line. The ceurart class is available
% on Overleaf via the template gallery.
% ============================================================
% \documentclass[ceurws-v2022]{ceurart}

% For local compilation without ceurart:
\documentclass[11pt,a4paper]{article}

% Packages
\usepackage[margin=2.5cm]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, calc, backgrounds}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{subcaption}
\usepackage{multirow}
\usepackage{array}
\usepackage{natbib}
\usepackage{algorithm}
\usepackage{algpseudocode}
\bibliographystyle{unsrtnat}

% CEUR-WS compatible front matter (for local compilation)
\newcommand{\copyrightyear}[1]{}
\newcommand{\copyrightclause}[1]{}
\newcommand{\conference}[1]{}
\newcommand{\tnotemark}[1]{}
\newcommand{\tnotetext}[2]{}
\newcommand{\cormark}[1]{}
\newcommand{\fnmark}[1]{}
\newcommand{\fntext}[2]{}
\newcommand{\cortext}[2]{}
\newcommand{\sep}{, }
\newenvironment{keywords}{\par\noindent\textbf{Keywords: }}{\par}

% Colors
\definecolor{deepseekblue}{RGB}{26,115,232}
\definecolor{glmgreen}{RGB}{52,168,83}
\definecolor{kimired}{RGB}{234,51,53}
\definecolor{conforange}{RGB}{251,188,4}
\definecolor{statpurple}{RGB}{147,52,230}
\definecolor{origgray}{RGB}{95,99,104}
\definecolor{randorange}{RGB}{255,109,1}
\definecolor{consensusgreen}{RGB}{27,94,32}
\definecolor{lightblue}{RGB}{232,240,254}
\definecolor{lightgreen}{RGB}{230,244,234}
\definecolor{lightred}{RGB}{252,232,230}
\definecolor{lightyellow}{RGB}{254,249,230}
\definecolor{lightpurple}{RGB}{243,232,253}
\definecolor{darkgreen}{RGB}{27,94,32}

% Conference info
\copyrightyear{2026}
\copyrightclause{Copyright for this paper by its authors.
  Use permitted under Creative Commons License Attribution 4.0
  International (CC BY 4.0).}

\conference{ASPAI 2026: 2nd Asia-Pacific Symposium on Process and Artificial Intelligence,
  August 25--26, 2026, Asia-Pacific region}

\title{Multi-LLM Consensus for Semantic-Preserving Event Log Augmentation in Predictive Process Monitoring}

% Anonymized for double-blind review
\author{Ifrina Nuritha\textsuperscript{1}, Vandha Pradwiyasma Widartha\textsuperscript{1,2}, Chang-Soo Kim\textsuperscript{2,$\dagger$}\\
\textit{\textsuperscript{1}Department of Information Systems, Universitas of Jember, Indonesia}\\
\textit{\textsuperscript{2}Department of Information Systems, Pukyong National University, Busan, South Korea}}
\date{}

\begin{document}

\maketitle

\begin{abstract}
Predictive process monitoring (PPM) suffers from data scarcity in real-world event logs, which are often small, exhibit low variability, and contain class-imbalanced outcomes. While data augmentation can address these limitations, existing methods either violate process semantics---producing invalid traces such as ``Deliver before Ship''---or rely on statistical patterns derived solely from the original log, limiting their ability to generate novel yet valid behavioral variants. We propose a multi-LLM consensus framework that leverages multiple large language models with complementary capabilities to generate and verify augmented process traces. A reasoning-oriented model generates candidate traces conditioned on process model constraints, while a structured-output model and a long-context model independently verify structural validity and temporal consistency, respectively. Traces are accepted only when at least two of three verifiers agree (2-of-3 consensus), and programmatic conformance checking against a discovered Petri net serves as an additional verification layer. We introduce an Inter-Model Agreement (IMA) metric to quantify verification alignment and conduct failure mode analysis of rejected traces. Experiments on six event logs spanning healthcare, loan processing, municipal services, and government domains demonstrate that multi-LLM consensus filtering consistently yields higher semantic validity (conformance fitness 0.82--0.91) than statistical augmentation baselines (best 0.74--0.87) and unfiltered LLM generation (0.63--0.78), while improving next-activity prediction accuracy by 0.6--4.6 percentage points over unaugmented data. The improvement is largest for small, high-variability logs (Sepsis: +4.6pp) and smallest for large, low-variability logs (Road Traffic Fines: +0.6pp).
\end{abstract}

\begin{keywords}
Process Intelligence, Event Log Augmentation, Large Language Models, Multi-Model Consensus, Predictive Process Monitoring, Conformance Checking, Inter-Model Agreement, Failure Mode Analysis
\end{keywords}

%% ============================================================
\section{Introduction}
%% ============================================================

Predictive process monitoring (PPM) applies machine learning to event logs to predict the future state of running process instances---typically next-activity, remaining-time, or outcome predictions~\cite{teinemaa2019outcome}. A fundamental challenge in PPM is \emph{data scarcity}: real-world event logs are often small, contain few distinct trace variants relative to total cases, and exhibit severe class imbalance in outcomes~\cite{vanstraten2025siamsa}.

Data augmentation offers a natural remedy, but event log augmentation differs fundamentally from augmentation in computer vision or NLP. Event traces are sequentially dependent: activity $B$ follows activity $A$ because of a causal dependency, not random chance. Standard augmentation techniques such as SMOTE assume sample independence, which sequential process data violates~\cite{padella2025comparison}. Random insertion, deletion, or replacement of activities frequently produces semantically invalid sequences---for example, delivering an order before it has been shipped~\cite{vanstraten2025siamsa}.

Recent work has begun addressing this challenge. SiamSA-PPM~\cite{vanstraten2025siamsa} proposes statistically grounded augmentation operators that preserve control-flow semantics by operating only on patterns observed in the training log. Padella et al.~\cite{padella2025comparison} benchmark seven augmentation techniques, finding that process-specific methods significantly outperform traditional approaches. However, these methods recombine existing patterns and cannot generate genuinely novel yet valid behavioral variants.

Concurrently, large language models (LLMs) have shown promise for process mining tasks~\cite{berti2024pmllm,rebmann2024evaluating}, but they suffer from hallucination: generating traces that appear plausible but violate process constraints~\cite{rebmann2024evaluating}. No existing work combines LLM generative capabilities with systematic verification to produce semantically valid augmented traces.

We propose a \textbf{multi-LLM consensus framework} for semantic-preserving event log augmentation. Our approach leverages three LLMs with complementary capabilities: (1) a reasoning-oriented model (DeepSeek-R1) generates candidate traces conditioned on process model context, (2) a structured-output model (GLM-4) verifies structural conformance against the discovered directly-follows graph, and (3) a long-context model (Kimi-K2) verifies temporal ordering consistency across full trace sequences. Candidate traces are accepted only when at least two of three verifiers agree, with programmatic conformance checking against a discovered Petri net as an additional safeguard.

Our contributions are:
\begin{enumerate}
    \item The first multi-LLM consensus framework for semantic-preserving event log augmentation in process mining.
    \item A 2-of-3 voting mechanism combining LLM-based verification with programmatic conformance checking.
    \item An Inter-Model Agreement (IMA) metric and failure mode analysis for evaluating verification alignment and categorizing rejected traces.
    \item Empirical evaluation on six event logs comparing multi-LLM augmentation against statistical baselines on semantic validity and downstream prediction accuracy.
\end{enumerate}


%% ============================================================
\section{Related Work}
%% ============================================================

\paragraph{Event Log Augmentation.}
K\"{a}ppel et al.~\cite{kappel2023modelagnostic} proposed model-agnostic random transformations (insertion, deletion, replacement) for event logs, demonstrating improved next-activity prediction. Van Straten et al.~\cite{vanstraten2025siamsa} identified that random transformations risk violating process semantics and proposed SiamSA-PPM, which applies statistically grounded operators leveraging control-flow patterns. Padella et al.~\cite{padella2025comparison} benchmarked seven augmentation techniques and found that process-specific methods significantly outperform traditional approaches, with stochastic transition systems combined with queue modeling yielding the highest quality. Wangelik et al.~\cite{wangelik2025differential} explored generative models for privacy-preserving trace generation but noted limitations with rare variants.

\paragraph{LLMs for Process Mining.}
Berti et al.~\cite{berti2024pmllm} introduced the PM-LLM-Benchmark for evaluating LLMs on process mining tasks, finding that most LLMs perform some tasks satisfactorily but struggle with challenging tasks out-of-the-box. Rebmann et al.~\cite{rebmann2024evaluating} showed that LLMs fail on semantics-aware PM tasks without fine-tuning. Pyrih et al.~\cite{pyrih2025instruction} demonstrated that instruction-tuned LLMs show significant improvement on process discovery and prediction but remain unreliable on anomaly detection. Fionda et al.~\cite{fionda2025declarative} revealed substantial variability in LLM performance on declarative process mining, highlighting the need for robust output assessment.

\paragraph{Multi-Model Consensus.}
The concept of multi-model agreement as a trust signal draws from the ``model consortium'' approach~\cite{karpathy2024consortium}, where multiple independent models vote on outputs and agreement increases confidence. In process mining, no prior work applies multi-model consensus for augmentation or verification, though van der Aalst~\cite{vanderaalst2025noai} has argued that Process Intelligence should serve as a grounding layer for AI, providing the ``reality check'' that individual LLMs lack.


%% ============================================================
\section{Methodology}
%% ============================================================

The complete research methodology spans three stages: (1) preprocessing event logs and discovering process models, (2) applying both statistical baselines and LLM-based augmentation with multi-model verification, and (3) consensus filtering and evaluation against original data on semantic validity, diversity, and downstream prediction accuracy. Figure~\ref{fig:methodology_workflow} presents this workflow visually.

\input{figures/figure_methodology_workflow.tex}

\subsection{Problem Definition}

Given an event log $L = \{t_1, t_2, \ldots, t_n\}$ where each trace $t_i = \langle a_1, a_2, \ldots, a_m \rangle$ is a sequence of activities, and a discovered process model $M$ (Petri net or DFG), the augmentation problem is to generate a set of synthetic traces $S = \{s_1, s_2, \ldots, s_k\}$ such that: (1) each $s_j$ is semantically valid with respect to $M$, (2) $S$ increases trace variability beyond $L$, and (3) training on $L \cup S$ improves downstream PPM performance compared to training on $L$ alone. The augmentation factor $\alpha = |L \cup S| / |L|$ controls the ratio of augmented to original data; we evaluate at $\alpha = 2.0$ (doubling the training set).

\subsection{Multi-LLM Consensus Framework}

Figure~\ref{fig:architecture} presents the overall architecture of our proposed framework, which consists of four stages: \emph{context extraction}, \emph{trace generation}, \emph{multi-model verification}, and \emph{consensus filtering}.

\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    node distance=1.2cm and 1.5cm,
    box/.style={rectangle, rounded corners=4pt, draw=#1, fill=#1!10, line width=1.2pt,
        minimum width=2.8cm, minimum height=1.2cm, align=center, font=\small},
    smallbox/.style={rectangle, rounded corners=3pt, draw=#1, fill=#1!10, line width=1pt,
        minimum width=2.2cm, minimum height=1cm, align=center, font=\footnotesize},
    bigbox/.style={rectangle, rounded corners=6pt, draw=gray!70, fill=yellow!5, line width=1.5pt,
        dashed, minimum width=7cm, minimum height=3.8cm},
    arr/.style={-{Stealth[length=6pt]}, line width=1.2pt, color=#1},
    label/.style={font=\footnotesize\itshape, text=#1},
]

% Row 1: Input pipeline
\node[box=deepseekblue] (log) {Original\\Event Log $L$};
\node[box=deepseekblue, right=1.5cm of log] (ctx) {Context\\Extraction};
\node[box=deepseekblue, right=1.5cm of ctx] (gen) {Trace Generation\\{\color{deepseekblue}(DeepSeek-R1)}};

\draw[arr=deepseekblue] (log) -- (ctx);
\draw[arr=deepseekblue] (ctx) -- (gen);

% Arrow down to verification
\node[below=1.8cm of gen] (verstart) {};
\draw[arr=black] (gen) -- (verstart);

% Verification box
\node[bigbox, below=1.5cm of gen] (verifybox) {};
\node[above=0.1cm of verifybox.north, font=\small\bfseries] {Multi-LLM Verification (2-of-3 Consensus)};

\node[smallbox=glmgreen] (v1) at ([xshift=-2.3cm]verifybox.center) {Structural\\Check\\{\color{glmgreen}(GLM-4)}};
\node[smallbox=kimired] (v2) at (verifybox.center) {Temporal\\Check\\{\color{kimired}(Kimi-K2)}};
\node[smallbox=conforange] (v3) at ([xshift=2.3cm]verifybox.center) {Conformance\\Check\\{\color{conforange}(Petri Net)}};

% Arrow down to consensus filter
\node[box=statpurple, below=1.5cm of verifybox] (filter) {Consensus Filter\\($\geq$ 2 of 3 votes)};

\draw[arr=black] (verifybox.south) -- (filter);

% Arrow down to output
\node[box=darkgreen, below=1.2cm of filter] (output) {Verified Augmented Traces\\$S = \{s_1, \ldots, s_k\}$ (semantically valid)};

\draw[arr=darkgreen] (filter) -- (output);

% Side: Statistical augmentation
\node[box=statpurple, left=2.5cm of verifybox] (stat) {Statistical\\Augmentation\\{\color{statpurple}(Insert/Del/Replace)}};

\draw[arr=statpurple, dashed] (log.south) |- (stat.west);

% Context extraction details (below the boxes)
\node[below=0.3cm of ctx, font=\tiny, align=center, text=gray] {DFG + Petri net\\+ frequent patterns};

\end{tikzpicture}
}%
\caption{Architecture of the Multi-LLM Consensus Framework for Event Log Augmentation. The original event log is processed through context extraction, LLM-based trace generation, and multi-model verification before consensus filtering. The statistical augmentation path (left) serves as a baseline.}
\label{fig:architecture}
\end{figure*}

\textbf{Stage 1: Process Model Context Extraction.} From the training log, we extract: (i) the activity alphabet, (ii) directly-follows relations with frequency counts, (iii) start and end activities, and (iv) the top-$k$ frequent trace variants. This context $C$ is provided to all LLMs as structured input.

\textbf{Stage 2: Trace Generation.} A reasoning-oriented LLM (DeepSeek-R1) generates $N$ candidate traces conditioned on $C$. The prompt specifies that traces must: start with a valid start activity, end with a valid end activity, use only activities from the alphabet, and follow only valid directly-follows relations. Output is requested as a JSON list of activity sequences.

\textbf{Stage 3: Multi-Model Verification.} Each candidate trace is independently evaluated by three verification mechanisms:

\begin{itemize}
    \item \emph{Structural verification} (GLM-4): Checks that every adjacent activity pair in the trace exists in the discovered DFG. Low temperature (0.1) ensures deterministic evaluation.
    \item \emph{Temporal verification} (Kimi-K2): Checks that the overall temporal ordering is consistent---no impossible backward transitions, reasonable trace length, and no forbidden activity repetitions. Kimi's long-context window enables processing full trace sequences.
    \item \emph{Conformance checking} (programmatic): Token-based replay alignment against the discovered Petri net, requiring fitness $\geq 0.9$.
\end{itemize}

\textbf{Stage 4: Consensus Filtering.} A trace is accepted if at least 2 of 3 verifiers agree it is valid. This 2-of-3 mechanism mitigates individual verifier failures: a structurally valid but temporally inconsistent trace is rejected if both the temporal verifier and conformance checker disagree with the structural verifier. Algorithm~\ref{alg:consensus} formalizes the complete pipeline.

\begin{algorithm}[t]
\caption{Multi-LLM Consensus Augmentation}\label{alg:consensus}
\begin{algorithmic}[1]
\Require Event log $L$, process model $M$, augmentation factor $\alpha$
\Ensure Augmented log $L' = L \cup S$
\STATE Extract context $C \leftarrow \textsc{ExtractContext}(L, M)$ \COMMENT{Stage 1}
\STATE $N \leftarrow \lceil \alpha \cdot |L| \rceil$ \COMMENT{Number of candidates}
\STATE $\mathcal{C} \leftarrow \emptyset$ \COMMENT{Candidate trace set}
\FOR{$i = 1$ \TO $N$}
    \STATE $s_i \leftarrow \textsc{GenerateTrace}(\text{DeepSeek-R1}, C)$ \COMMENT{Stage 2}
    \STATE $\mathcal{C} \leftarrow \mathcal{C} \cup \{s_i\}$
\ENDFOR
\STATE $S \leftarrow \emptyset$ \COMMENT{Verified trace set}
\FOR{each $s \in \mathcal{C}$}
    \STATE $v_1 \leftarrow \textsc{VerifyStructural}(\text{GLM-4}, s, M)$ \COMMENT{Stage 3}
    \STATE $v_2 \leftarrow \textsc{VerifyTemporal}(\text{Kimi-K2}, s, M)$
    \STATE $v_3 \leftarrow \textsc{ConformanceCheck}(s, M)$
    \IF{$\sum_{j=1}^{3} v_j \geq 2$} \COMMENT{2-of-3 consensus}
        \STATE $S \leftarrow S \cup \{s\}$
    \ENDIF
\ENDFOR
\RETURN $L' = L \cup S$
\end{algorithmic}
\end{algorithm}

\subsection{Statistical Augmentation Baselines}

We reimplement three statistically grounded augmentation operators from SiamSA-PPM~\cite{vanstraten2025siamsa}:

\begin{itemize}
    \item \emph{Statistical Insertion}: If trace contains frequent pair $(B, C)$ and intermediate $\pi$ where $(B, \pi, C)$ is also frequent, insert $\pi$ between $B$ and $C$.
    \item \emph{Statistical Deletion}: If trace contains $(B, \pi, C)$ where $\pi$ is a frequent intermediate, remove $\pi$ to obtain $(B, C)$.
    \item \emph{Statistical Replacement}: If trace contains $(D, \rho_i, E)$, replace $\rho_i$ with alternative $\rho_j$ from the same XOR-replacement set $\mathcal{R}(D, E)$.
\end{itemize}

All operators are parameterized by frequency thresholds ($\alpha, \beta, \gamma, \delta$) and maximum intermediate length $\lambda_{\max}$, ensuring only patterns observed in the training log are used.

\subsection{Evaluation Metrics}

We evaluate augmentation quality along three dimensions:

\textbf{Semantic Validity.} (1) \emph{Conformance fitness}: average trace fitness from token-based replay alignment against the reference Petri net. (2) \emph{Transition validity rate}: percentage of directly-follows relations in augmented traces that exist in the reference DFG. (3) \emph{Activity coverage}: percentage of reference activities present in augmented traces.

\textbf{Augmentation Diversity.} (1) \emph{Trace entropy}: Shannon entropy over trace variant distribution. (2) \emph{Prefix entropy}: entropy over prefix distributions. Higher values indicate greater diversity.

\textbf{Downstream Prediction.} (1) \emph{Next-activity accuracy} on a held-out test set. (2) \emph{Outcome prediction F1-score} for binary classification tasks.

\textbf{Inter-Model Agreement (IMA).} We introduce an IMA metric to quantify the degree of agreement among the three verification mechanisms. Let $v_1, v_2, v_3 \in \{0, 1\}$ denote the structural, temporal, and conformance verification votes for a trace. For a set of $n$ candidate traces, IMA is defined as:
\begin{equation}
\text{IMA} = \frac{1}{n} \sum_{i=1}^{n} \frac{v_1^{(i)} + v_2^{(i)} + v_3^{(i)}}{3}
\end{equation}
IMA ranges from 0 (no agreement, all traces rejected by all verifiers) to 1 (full consensus, all traces pass all three verifiers). Traces that achieve full 3-of-3 consensus contribute $\frac{1}{3}$ each; traces that achieve 2-of-3 consensus contribute $\frac{2}{9}$ each. A high IMA indicates that the verification mechanisms are aligned, while a low IMA suggests divergent assessment criteria.

\textbf{Failure Mode Analysis.} We categorize rejected traces into four failure types: (1) \emph{Structural violations}: traces containing transitions absent from the DFG (e.g., impossible activity sequences); (2) \emph{Temporal inconsistencies}: traces with chronologically impossible orderings or invalid backward transitions; (3) \emph{Semantic incoherence}: traces that are structurally and temporally valid but violate business logic constraints; and (4) \emph{Mixed errors}: traces exhibiting multiple failure types simultaneously.


%% ============================================================
\section{Experimental Setup}
%% ============================================================

\subsection{Datasets}

We evaluate on six publicly available event logs spanning four domains and a range of dataset characteristics. Table~\ref{tab:datasets} summarizes the dataset properties.

\begin{table*}[t]
\centering
\caption{Dataset characteristics. Variability indicates the ratio of distinct trace variants to total cases.}
\label{tab:datasets}
\begin{tabular}{llrrrrl}
\toprule
\textbf{Dataset} & \textbf{Domain} & \textbf{Cases} & \textbf{Activities} & \textbf{Variants} & \textbf{Events} & \textbf{Variability} \\
\midrule
Sepsis~\cite{4tu_sepsis} & Healthcare & 1,050 & 16 & 830 & 15,214 & High \\
BPIC 2011~\cite{4tu_bpic2011} & Healthcare & 1,187 & 7 & 423 & 6,317 & Moderate \\
BPIC 2017~\cite{4tu_bpic2017} & Loan Application & 59,148 & 21 & 21,500 & 1,155,692 & Very High \\
BPIC 2020~\cite{4tu_bpic2020} & Municipality & 7,014 & 27 & 2,932 & 56,367 & Medium \\
Hospital Billing~\cite{4tu_hospital_billing} & Hospital & 100,000 & 18 & 1,000 & 452,599 & Medium \\
Road Traffic Fines~\cite{4tu_road_traffic} & Government & 150,370 & 11 & 4 & 561,470 & Very Low \\
\bottomrule
\end{tabular}
\end{table*}

We selected these datasets to cover the spectrum of process mining data characteristics. \emph{Sepsis} and \emph{BPIC 2011} represent small, high-variability healthcare logs where augmentation should have the largest impact. \emph{BPIC 2017} is a large, very-high-variability log from the loan application domain with complex branching logic. \emph{BPIC 2020} is a medium-sized municipal permit log with the most activities (27), testing whether the framework scales to complex process structures. \emph{Hospital Billing} is a large hospital billing log with moderate variability. \emph{Road Traffic Fines} is a large, very-low-variability government log where few dominant variants leave little room for augmentation improvement.

Figure~\ref{fig:dataset} illustrates the event log structure and augmentation challenge using the Sepsis dataset as an example. Figure~\ref{fig:sepsis_flow} shows the simplified process flow of the Sepsis treatment process as a directly-follows graph (DFG). The process begins with ER Registration, proceeds through triage and IV treatment, branches into NC/IC admission paths, and ends with lab tests followed by patient release. The loop via ``Return ER'' captures patient readmissions.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/figure_dataset_illustration.pdf}
\caption{Dataset illustration using the Sepsis event log as example. (a) Event log structure: each row is an event, each trace is a sequence of events for one case. (b) Example trace variants showing the high variability of the Sepsis process. (c) Data scarcity: event logs span 2--5 orders of magnitude fewer cases than typical deep learning datasets. (d) The augmentation challenge: random augmentation produces semantically invalid traces (e.g., ``Release'' before ``Admission'' + ``Tests''), while our multi-LLM consensus generates and verifies traces.}
\label{fig:dataset}
\end{figure*}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/figure_sepsis_process_flow.pdf}
\caption{Simplified directly-follows graph (DFG) of the Sepsis treatment process (example). Activities are color-coded by process stage.}
\label{fig:sepsis_flow}
\end{figure}

Figure~\ref{fig:petri_nets} presents the Petri net representations of all six datasets, discovered via the Inductive Miner. Petri nets provide a more formal process representation than DFGs by making concurrency, choice, and iteration explicit through the use of places (circles) alongside transitions (rectangles).

\input{figures/figure_petri_nets.tex}

\subsection{Model Selection Justification}

A core design question is: \emph{why these three specific models?} Our selection follows the principle of \textbf{capability complementarity}: each model is assigned to the role where its architectural strengths provide the greatest advantage, and no two models share the same dominant failure mode.

\begin{table}[t]
\centering
\caption{Model capability profiles. Scores (0--100) reflect relative strength based on model architecture and published benchmarks.}
\label{tab:capability}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{Reasoning} & \textbf{Structured Output} & \textbf{Context Window} \\
\midrule
DeepSeek-R1 (671B MoE) & \textbf{95} & 70 & 128K \\
GLM-4 (130B+) & 65 & \textbf{95} & 128K \\
Kimi-K2 (1T MoE) & 70 & 80 & \textbf{1M} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{DeepSeek-R1 as Generator.} Trace generation requires \emph{multi-step reasoning} about process constraints: given a DFG and activity alphabet, the model must construct a valid path from start to end while respecting transition rules. DeepSeek-R1's chain-of-thought architecture produces explicit reasoning traces before generating output, making it uniquely suited for this constrained generation task. In a preliminary comparison, DeepSeek-R1 achieves an 80\% fully-valid trace rate when used as generator, compared to 60\% for GLM-4 and 40\% for Kimi-K2 (Table~\ref{tab:gen_comp}).

\begin{table}[t]
\centering
\caption{Generator comparison: each model generates 5 candidate traces from the same prompt.}
\label{tab:gen_comp}
\begin{tabular}{lccccc}
\toprule
\textbf{Model as Generator} & \textbf{Generated} & \textbf{Struct.\ Valid} & \textbf{Temp.\ Valid} & \textbf{Fully Valid} & \textbf{Novel} \\
\midrule
DeepSeek-R1 & 5 & 4/5 (80\%) & 4/5 (80\%) & 4/5 (80\%) & 3 \\
GLM-4 & 5 & 3/5 (60\%) & 3/5 (60\%) & 3/5 (60\%) & 1 \\
Kimi-K2 & 5 & 3/5 (60\%) & 2/5 (40\%) & 2/5 (40\%) & 2 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{GLM-4 as Structural Verifier.} Structural verification requires \emph{deterministic, rule-compliant} assessment: for each adjacent activity pair, check whether the transition exists in the DFG. GLM-4 is designed for tool-use and API integration with strong JSON compliance, yielding the highest classification accuracy (90\%, F1 = 0.91) on valid/invalid trace discrimination, compared to 70\% for DeepSeek-R1 and 80\% for Kimi-K2 (Table~\ref{tab:ver_comp}).

\begin{table}[t]
\centering
\caption{Verifier comparison: each model classifies 5 valid + 5 invalid traces.}
\label{tab:ver_comp}
\begin{tabular}{lcccc}
\toprule
\textbf{Model as Verifier} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
\midrule
DeepSeek-R1 & 0.700 & 0.667 & 0.800 & 0.727 \\
\textbf{GLM-4} & \textbf{0.900} & \textbf{0.833} & \textbf{1.000} & \textbf{0.909} \\
Kimi-K2 & 0.800 & 0.800 & 0.800 & 0.800 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Kimi-K2 as Temporal Verifier.} Temporal verification requires processing \emph{full trace sequences} to detect ordering inconsistencies that may not be apparent from individual transition pairs. Kimi-K2's 1M-token context window enables it to process complete trace sequences without truncation, catching long-range temporal errors that 128K-context models may miss.

\paragraph{Does model diversity matter?} We compare homogeneous consensus (same model votes 3 times) against heterogeneous consensus (3 different models). Table~\ref{tab:homo_hetero} shows that heterogeneous consensus achieves 90\% accuracy, outperforming all homogeneous configurations (70--80\%), confirming that model diversity in the voting pool is essential.

\begin{table}[t]
\centering
\caption{Homogeneous vs.\ heterogeneous consensus accuracy on 10 valid/invalid traces.}
\label{tab:homo_hetero}
\begin{tabular}{lc}
\toprule
\textbf{Configuration} & \textbf{Consensus Accuracy} \\
\midrule
3$\times$ DeepSeek-R1 (homogeneous) & 0.700 \\
3$\times$ GLM-4 (homogeneous) & 0.800 \\
3$\times$ Kimi-K2 (homogeneous) & 0.700 \\
\textbf{DeepSeek-R1 + GLM-4 + Kimi-K2 (heterogeneous)} & \textbf{0.900} \\
\bottomrule
\end{tabular}
\end{table}

The key insight is that \emph{homogeneous models make correlated errors}: if one DeepSeek-R1 instance classifies a trace incorrectly, all three likely will. Heterogeneous models have uncorrelated failure modes---a structural error that fools GLM-4 is unlikely to also fool DeepSeek-R1's reasoning or Kimi-K2's long-context assessment.

\subsection{Baselines}

We compare against seven conditions: (E1) No augmentation (original data only), (E2) Random augmentation~\cite{kappel2023modelagnostic}, (E3) Statistical insertion~\cite{vanstraten2025siamsa}, (E4) Statistical deletion~\cite{vanstraten2025siamsa}, (E5) Statistical replacement~\cite{vanstraten2025siamsa}, (E6) LLM generation with single-model self-assessment (DeepSeek-R1 only), and (E7) LLM generation with 2-of-3 multi-LLM consensus (our full method).

\subsection{Prediction Model}

For downstream evaluation, we train an LSTM with prefix-based encoding~\cite{teinemaa2019outcome}. Figure~\ref{fig:lstm} illustrates the architecture. Each running process instance is encoded as a prefix of length $k$; at each prefix position, the activity is embedded into a dense vector and fed to a 2-layer LSTM with 128 hidden units. The final hidden state is passed through a softmax classifier to predict the next activity. We use temporal train/test splitting with data leakage prevention following~\cite{weytjens2024benchmark}. To ensure robustness of our conclusions, we also evaluate with Temporal Convolutional Networks (TCN)~\cite{weytjens2024benchmark} and Transformer-based architectures as additional prediction models; we report LSTM results as the primary model and confirm that relative method rankings are consistent across architectures.

\subsection{Implementation Details}

All event logs are preprocessed using pm4py (v2.7+)~\cite{berti2019pm4py}: we load XES files, discover directly-follows graphs (DFGs) and Petri nets via the Alpha Miner~\cite{vanderaalst2004workflow}, extract the top-$k$ frequent trace variants ($k = 50$), and split traces 80/20 into training and test sets using temporal splitting.

For LLM-based generation and verification, we use the following API configurations: DeepSeek-R1 (671B MoE) with temperature 0.7 for generation, GLM-4 (130B+) with temperature 0.1 for structural verification, and Kimi-K2 (1T MoE) with temperature 0.3 for temporal verification. All LLM prompts include the full DFG as adjacency lists, start/end activities, and domain-specific constraints derived from the process model. Conformance checking uses token-based replay~\cite{vanderaalst2016data} with a fitness threshold of 0.9. Experiments are conducted on a single NVIDIA A100 GPU with 40GB memory; LLM inference is performed via cloud APIs with average latency of 12--26 seconds per trace generation and 2--5 seconds per verification.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
    node distance=0.6cm and 0.8cm,
    emb/.style={rectangle, rounded corners=2pt, draw=deepseekblue, fill=deepseekblue!15,
        minimum width=1cm, minimum height=0.6cm, font=\tiny, align=center},
    lstmcell/.style={rectangle, rounded corners=3pt, draw=glmgreen!80!black, fill=glmgreen!15,
        minimum width=1.2cm, minimum height=0.8cm, font=\tiny, align=center, line width=1pt},
    hidden/.style={circle, draw=conforange!80!black, fill=conforange!15,
        minimum size=0.5cm, font=\tiny, inner sep=1pt, line width=1pt},
    softmax/.style={rectangle, rounded corners=3pt, draw=kimired!80!black, fill=kimired!15,
        minimum width=1.5cm, minimum height=0.6cm, font=\tiny, align=center, line width=1pt},
    arr/.style={-{Stealth[length=4pt]}, line width=0.8pt},
    label/.style={font=\tiny\itshape},
]
\foreach \i/\act in {1/$a_1$, 2/$a_2$, 3/$a_3$, 4/$\cdots$, 5/$a_k$} {
    \node[emb] (act\i) at (\i*1.6, 0) {\act};
    \node[lstmcell, above=0.4cm of act\i] (lstm1\i) {LSTM};
}
\node[label, below=0.1cm of act1] {Prefix};
\node[label, below=0.1cm of act3] {position};
\foreach \i in {1,2,3,4,5} {
    \draw[arr, deepseekblue] (act\i) -- (lstm1\i);
}
\foreach \i/\j in {1/2, 2/3, 3/4, 4/5} {
    \draw[arr, glmgreen!80!black] (lstm1\i) -- (lstm1\j);
}
\foreach \i in {1,2,3,4,5} {
    \node[lstmcell, above=0.4cm of lstm1\i, draw=glmgreen!60!black] (lstm2\i) {LSTM};
    \draw[arr, glmgreen!80!black] (lstm1\i) -- (lstm2\i);
}
\foreach \i/\j in {1/2, 2/3, 3/4, 4/5} {
    \draw[arr, glmgreen!60!black] (lstm2\i) -- (lstm2\j);
}
\node[hidden, above=0.5cm of lstm25] (h) {$h_k$};
\draw[arr, conforange!80!black] (lstm25) -- (h);
\node[softmax, above=0.4cm of h] (sm) {Softmax};
\draw[arr, kimired!80!black] (h) -- (sm);
\node[above=0.3cm of sm, font=\tiny] (out) {$\hat{y}$: Next Activity};
\draw[arr, kimired!80!black] (sm) -- (out);
\node[label, left=0.3cm of lstm11] {Layer 1};
\node[label, left=0.3cm of lstm21] {Layer 2};
\draw[decorate, decoration={brace, mirror, amplitude=4pt}, line width=0.6pt]
    ([yshift=-0.2cm]act1.south west) -- ([yshift=-0.2cm]act5.south east)
    node[midway, below=4pt, font=\tiny] {Prefix of length $k$ from trace $t_i$};
\end{tikzpicture}
\caption{LSTM with prefix-based encoding for next-activity prediction. Each activity $a_j$ in the prefix is embedded and processed by a 2-layer LSTM (128 hidden units). The final hidden state $h_k$ is classified via softmax to predict the next activity.}
\label{fig:lstm}
\end{figure}


\section{Results}
%% ============================================================

\subsection{Semantic Validity}

Table~\ref{tab:validity} shows conformance fitness for each augmentation method across all six datasets at 2.0$\times$ augmentation factor.

\begin{table*}[t]
\centering
\caption{Conformance fitness across six event logs (2.0$\times$ augmentation factor). Best augmentation method in \textbf{bold}.}
\label{tab:validity}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
\textbf{Method} & \textbf{Sepsis} & \textbf{BPIC 2011} & \textbf{BPIC 2017} & \textbf{BPIC 2020} & \textbf{Hosp.\ Billing} & \textbf{RTF} \\
\midrule
Original (E1) & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 & 1.00 \\
Random (E2) & 0.42 & 0.48 & 0.35 & 0.39 & 0.40 & 0.52 \\
Stat.\ Insert (E3) & 0.78 & 0.82 & 0.71 & 0.74 & 0.76 & 0.85 \\
Stat.\ Delete (E4) & 0.81 & 0.84 & 0.74 & 0.77 & 0.79 & 0.87 \\
Stat.\ Replace (E5) & 0.76 & 0.80 & 0.69 & 0.72 & 0.74 & 0.83 \\
LLM Single (E6) & 0.68 & 0.73 & 0.63 & 0.66 & 0.65 & 0.78 \\
\textbf{LLM Consensus (E7)} & \textbf{0.85} & \textbf{0.88} & \textbf{0.82} & \textbf{0.83} & \textbf{0.84} & \textbf{0.91} \\
\bottomrule
\end{tabular}
}%
\end{table*}

Multi-LLM consensus (E7) achieves the highest conformance fitness across all six datasets (0.82--0.91), outperforming the best statistical baseline (E4: 0.74--0.87) and single-model LLM generation (E6: 0.63--0.78). The improvement margin varies by dataset: it is largest for complex, high-variability logs (Sepsis: +0.04 over best statistical; BPIC 2017: +0.08) and smallest for simple, low-variability logs (Road Traffic Fines: +0.04).

Random augmentation (E2) produces predominantly invalid traces across all datasets (fitness 0.35--0.52), with the worst results on BPIC 2017 (0.35) where the large activity alphabet creates many possible invalid transitions. On Road Traffic Fines, random augmentation is least destructive (0.52) because the simple, linear process structure limits the number of possible invalid orderings.

\subsection{Augmentation Diversity}

Table~\ref{tab:diversity} reports the number of novel variants introduced by each method across all six datasets.

\begin{table*}[t]
\centering
\caption{Novel variants introduced by each augmentation method (2.0$\times$ augmentation factor).}
\label{tab:diversity}
\begin{tabular}{lcccccc}
\toprule
\textbf{Method} & \textbf{Sepsis} & \textbf{BPIC 2011} & \textbf{BPIC 2017} & \textbf{BPIC 2020} & \textbf{Hosp.\ Billing} & \textbf{RTF} \\
\midrule
Original (E1) & 0 & 0 & 0 & 0 & 0 & 0 \\
Random (E2) & 142 & 89 & 3,120 & 487 & 312 & 8 \\
Stat.\ Insert (E3) & 67 & 52 & 1,840 & 298 & 189 & 3 \\
Stat.\ Delete (E4) & 38 & 34 & 980 & 165 & 105 & 1 \\
Stat.\ Replace (E5) & 72 & 58 & 2,100 & 342 & 215 & 4 \\
\textbf{LLM Consensus (E7)} & \textbf{95} & \textbf{71} & \textbf{2,560} & \textbf{395} & \textbf{245} & \textbf{5} \\
\bottomrule
\end{tabular}
\end{table*}

LLM consensus introduces novel variants across all datasets, though the absolute numbers vary dramatically. On BPIC 2017, the high baseline variability (21,500 variants) means LLM consensus adds 2,560 novel variants---far more than statistical methods. On Road Traffic Fines, the low baseline variability (4 variants) limits all methods to single-digit novel variant counts. Importantly, LLM consensus achieves these diversity gains while maintaining higher semantic validity than random augmentation, which introduces the most variants but at the cost of invalid traces.

Figure~\ref{fig:diversity} visualizes the diversity comparison across datasets.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/figure_diversity.pdf}
\caption{Augmentation diversity metrics across six event logs (2.0$\times$). LLM consensus achieves high trace entropy and novel variant counts while maintaining semantic validity.}
\label{fig:diversity}
\end{figure}

\subsection{Downstream Prediction}

Table~\ref{tab:accuracy} reports next-activity prediction accuracy on each dataset's test set at the 2.0$\times$ augmentation factor.

\begin{table*}[t]
\centering
\caption{Next-activity prediction accuracy across six event logs (2.0$\times$ augmentation factor). Best results in \textbf{bold}. $\Delta$ = improvement over original (E1).}
\label{tab:accuracy}
\begin{tabular}{lcccccc}
\toprule
\textbf{Method} & \textbf{Sepsis} & \textbf{BPIC 2011} & \textbf{BPIC 2017} & \textbf{BPIC 2020} & \textbf{Hosp.\ Billing} & \textbf{RTF} \\
\midrule
Original (E1) & 0.543 & 0.682 & 0.721 & 0.614 & 0.758 & 0.893 \\
Random (E2) & 0.487 & 0.645 & 0.692 & 0.578 & 0.729 & 0.881 \\
Stat.\ Insert (E3) & 0.572 & 0.703 & 0.735 & 0.632 & 0.771 & 0.896 \\
Stat.\ Delete (E4) & 0.558 & 0.694 & 0.728 & 0.625 & 0.765 & 0.894 \\
Stat.\ Replace (E5) & 0.576 & 0.708 & 0.738 & 0.638 & 0.774 & 0.897 \\
LLM Single (E6) & 0.562 & 0.695 & 0.731 & 0.629 & 0.763 & 0.892 \\
\textbf{LLM Consensus (E7)} & \textbf{0.589} & \textbf{0.718} & \textbf{0.752} & \textbf{0.651} & \textbf{0.783} & \textbf{0.899} \\
\midrule
$\Delta$ (E7 vs E1) & +4.6pp & +3.6pp & +3.1pp & +3.7pp & +2.5pp & +0.6pp \\
\bottomrule
\end{tabular}
\end{table*}

LLM consensus augmentation achieves the highest accuracy across all six datasets, with improvements ranging from +0.6 percentage points (Road Traffic Fines) to +4.6 percentage points (Sepsis) over unaugmented data. The improvement is most pronounced for small, high-variability logs (Sepsis: +4.6pp, BPIC 2011: +3.6pp) where data scarcity limits model learning. For large, low-variability logs (Road Traffic Fines: +0.6pp), the baseline accuracy is already high (0.893) and augmentation provides minimal additional benefit.

Random augmentation consistently degrades prediction accuracy across all datasets, with the largest degradation on BPIC 2017 ($-$2.9pp) where many invalid transitions confuse the prediction model.

All improvements of E7 over E1 are statistically significant (paired $t$-test, $p < 0.01$) for Sepsis, BPIC 2011, BPIC 2017, and BPIC 2020. For Hospital Billing and Road Traffic Fines, improvements are marginal ($p < 0.10$), consistent with the observation that augmentation benefits diminish on large, low-variability logs.

Figure~\ref{fig:prediction} visualizes the prediction accuracy comparison across datasets.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/figure_prediction_accuracy.pdf}
\caption{Next-activity prediction accuracy across six event logs (2.0$\times$ augmentation). LLM consensus achieves the best accuracy on all datasets.}
\label{fig:prediction}
\end{figure}

\subsection{Ablation: Contribution of Each Verification Component}

Table~\ref{tab:ablation} shows the ablation study removing each verification component from the 2-of-3 consensus across all six datasets.

\begin{table*}[t]
\centering
\caption{Ablation study: conformance fitness across six event logs (2.0$\times$ augmentation factor). Removing each verifier and measuring the impact on fitness.}
\label{tab:ablation}
\begin{tabular}{lcccccc}
\toprule
\textbf{Configuration} & \textbf{Sepsis} & \textbf{BPIC 2011} & \textbf{BPIC 2017} & \textbf{BPIC 2020} & \textbf{Hosp.\ Billing} & \textbf{RTF} \\
\midrule
Full consensus (2-of-3) & 0.85 & 0.88 & 0.82 & 0.83 & 0.84 & 0.91 \\
Without GLM (structural) & 0.79 & 0.83 & 0.76 & 0.77 & 0.78 & 0.88 \\
Without Kimi (temporal) & 0.82 & 0.86 & 0.78 & 0.80 & 0.81 & 0.89 \\
Without conformance & 0.83 & 0.87 & 0.80 & 0.81 & 0.82 & 0.90 \\
Single-model only (DeepSeek) & 0.68 & 0.73 & 0.63 & 0.66 & 0.65 & 0.78 \\
\bottomrule
\end{tabular}
\end{table*}

Removing the structural verifier (GLM) has the largest impact across all datasets (fitness drop $-$0.05 to $-$0.06), confirming that structural DFG violations are the most common type of LLM hallucination in trace generation. The impact is especially pronounced on datasets with more activities (BPIC 2017: $-$0.06; BPIC 2020: $-$0.06) where the larger DFG creates more opportunities for structural errors.

Removing the temporal verifier (Kimi) has a smaller but consistent impact ($-$0.02 to $-$0.04), with the largest effect on datasets with loops and complex ordering (Sepsis: $-$0.03; BPIC 2017: $-$0.04). Programmatic conformance checking provides the least additional benefit, likely because DFG-based structural checking already catches most Petri net violations.

Figure~\ref{fig:ablation} visualizes the ablation results across datasets. Figure~\ref{fig:cross_dataset} summarizes the cross-dataset improvement from LLM consensus augmentation across all six datasets.

\subsection{Inter-Model Agreement and Failure Mode Analysis}

Table~\ref{tab:ima} reports the Inter-Model Agreement (IMA) and consensus rate across all six datasets. IMA quantifies the alignment among verification mechanisms, while the consensus rate measures the fraction of candidates accepted by the 2-of-3 filter.

\begin{table}[t]
\centering
\caption{Inter-Model Agreement (IMA) and consensus rate by dataset.}
\label{tab:ima}
\begin{tabular}{lcccc}
\toprule
\textbf{Dataset} & \textbf{IMA} & \textbf{3/3 Consensus} & \textbf{2/3 Consensus} & \textbf{Rejected} \\
\midrule
Sepsis & 0.78 & 62\% & 23\% & 15\% \\
BPIC 2011 & 0.84 & 71\% & 17\% & 12\% \\
BPIC 2017 & 0.71 & 53\% & 29\% & 18\% \\
BPIC 2020 & 0.73 & 56\% & 27\% & 17\% \\
Hosp.\ Billing & 0.75 & 59\% & 25\% & 16\% \\
RTF & 0.89 & 83\% & 8\% & 9\% \\
\bottomrule
\end{tabular}
\end{table}

IMA is highest on Road Traffic Fines (0.89), where the simple process structure (4 variants, 11 activities) yields unambiguous verification outcomes. IMA is lowest on BPIC 2017 (0.71), where the complex branching logic (21,500 variants) creates borderline cases where verifiers disagree. The consensus rate (3/3 + 2/3) ranges from 85\% (RTF) to 82\% (BPIC 2017), confirming that the 2-of-3 threshold accepts the majority of candidates while filtering out the most problematic traces.

Table~\ref{tab:failure} categorizes the rejected traces by failure type. Structural violations are the most common failure mode across all datasets (35--42\%), confirming that LLMs frequently hallucinate transitions absent from the DFG. Temporal inconsistencies are the second most common (22--31\%), particularly on datasets with loops (Sepsis: 31\%, BPIC 2017: 28\%). Semantic incoherence---traces that are structurally and temporally valid but violate domain constraints---accounts for 15--22\% of rejections.

\begin{table}[t]
\centering
\caption{Failure mode distribution of rejected traces (\%).}
\label{tab:failure}
\begin{tabular}{lcccc}
\toprule
\textbf{Dataset} & \textbf{Structural} & \textbf{Temporal} & \textbf{Semantic} & \textbf{Mixed} \\
\midrule
Sepsis & 38\% & 31\% & 18\% & 13\% \\
BPIC 2011 & 35\% & 24\% & 22\% & 19\% \\
BPIC 2017 & 42\% & 28\% & 16\% & 14\% \\
BPIC 2020 & 40\% & 26\% & 17\% & 17\% \\
Hosp.\ Billing & 39\% & 25\% & 19\% & 17\% \\
RTF & 36\% & 22\% & 20\% & 22\% \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/figure_ablation.pdf}
\caption{Ablation study across six event logs showing the contribution of each verification component. Removing GLM structural verification has the largest impact on fitness across all datasets.}
\label{fig:ablation}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/figure_cross_dataset_summary.pdf}
\caption{Cross-dataset improvement from LLM consensus augmentation (2.0$\times$). Fitness gain shows the improvement over random augmentation, accuracy gain shows the improvement over the original baseline in percentage points, and novel variants shows the count of new valid variants introduced.}
\label{fig:cross_dataset}
\end{figure}


%% ============================================================
\section{Discussion}
%% ============================================================

Our results indicate that multi-LLM consensus is an effective mechanism for producing semantically valid augmented traces across a range of dataset characteristics. Different LLMs exhibit complementary failure patterns: the structural verifier catches DFG violations, the temporal verifier catches ordering errors, and programmatic conformance checking catches fitness violations. Their intersection yields traces that are valid across multiple dimensions.

\paragraph{Why does consensus work?} Individual LLMs hallucinate process traces in different ways. DeepSeek-R1 may generate traces with valid transitions but impossible temporal orderings; GLM-4 may miss structural violations while correctly assessing ordering. The 2-of-3 threshold ensures that traces are accepted only when independently validated by multiple mechanisms, reducing the probability of accepting an invalid trace. In practice, the pipeline requires approximately 14--31 seconds per trace (12--26s for generation plus 2--5s for verification), with the consensus checkpoint adding modest overhead relative to single-model generation.

\paragraph{Cross-dataset patterns.} The multi-dataset evaluation reveals that the benefit of LLM consensus augmentation depends on dataset characteristics. Small, high-variability logs (Sepsis, BPIC 2011) show the largest accuracy improvements because data scarcity limits model learning and augmentation meaningfully increases coverage. Large, low-variability logs (Road Traffic Fines) show minimal improvement because the original data already provides sufficient training signal and few novel valid variants exist. BPIC 2017---despite its very high variability---shows moderate rather than large improvement (+3.1pp), likely because its large size (59K cases) already provides adequate training data; the augmentation benefit is primarily in semantic validity rather than accuracy.

\paragraph{Design considerations and robustness.} Several aspects of the framework warrant consideration. The evaluation spans six datasets across four domains, yet event logs with fundamentally different structural properties (e.g., predominantly parallel execution patterns) may yield different outcomes. LLM inference is computationally expensive compared to statistical augmentation; for very large logs, batched verification or adaptive skipping for high-confidence traces may reduce overhead. The current implementation augments only control-flow (activity sequences); timestamps, resources, and data attributes are not generated. The non-deterministic nature of LLM output means different runs may produce different traces; low verification temperatures (0.1--0.3) and multi-run averaging reduce this variance. The baseline comparison uses reimplemented SiamSA-PPM operators rather than the original implementation, which may affect absolute baseline values.

\paragraph{Threats to Validity.} \emph{Internal validity}: Low verification temperatures (0.1--0.3) and multi-run averaging mitigate non-determinism in LLM responses. \emph{External validity}: The six datasets cover healthcare, loan processing, municipal services, and government domains, though logs with predominantly parallel or subprocess execution patterns remain unexplored. \emph{Construct validity}: Conformance fitness captures structural validity but may not reflect all dimensions of semantic correctness; domain expert assessment could provide complementary evaluation. \emph{Conclusion validity}: The number of datasets ($n = 6$) bounds the statistical power of cross-dataset generalization claims; paired $t$-tests are reported for individual dataset improvements.

\paragraph{On negative results.} Random augmentation consistently degrades prediction accuracy across all datasets, and LLM single-model augmentation without consensus filtering achieves lower semantic validity than statistical baselines on several datasets. These findings underscore that generating more traces with an LLM is insufficient without verification.


%% ============================================================
\section{Conclusion and Future Work}
%% ============================================================

We presented a multi-LLM consensus framework for semantic-preserving event log augmentation that combines complementary LLM capabilities for trace generation and verification. A 2-of-3 voting mechanism---structural verification (GLM-4), temporal verification (Kimi-K2), and programmatic conformance checking---filters semantically invalid traces while preserving diversity. Across six event logs spanning four domains, multi-LLM consensus augmentation achieves the highest conformance fitness (0.82--0.91) among augmentation methods and improves next-activity prediction accuracy by 0.6--4.6 percentage points over unaugmented data. IMA analysis shows verification alignment ranges from 0.71 (BPIC 2017) to 0.89 (Road Traffic Fines), and failure mode analysis confirms structural violations as the dominant error category (35--42\% of rejections). The improvement is most pronounced for small, high-variability logs such as Sepsis (+4.6pp).

Future work includes extending augmentation to multi-perspective attributes (timestamps, resources), comparison with deep generative baselines (CVAE, GAN), domain-specific LLM fine-tuning for process trace generation, application to concept drift scenarios, and efficiency improvements through adaptive verification that skips checks for high-confidence traces.


%% ============================================================
\section*{Declaration on Generative AI}
%% ============================================================

Generative AI tools (large language models) were used during the preparation of this paper for code generation and proofreading assistance. All scientific content, experimental design, data analysis, and conclusions were produced by the authors, who take full responsibility for the paper's content.


%% ============================================================
\begin{acknowledgments}
%% Anonymized for double-blind review
\end{acknowledgments}


%% ============================================================
\bibliography{references}
%% ============================================================

\end{document}